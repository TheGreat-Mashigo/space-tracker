version: 1
applications:
  - backend:
      phases:
        preBuild:
          commands:
            - '# Execute Amplify CLI with the helper script'
            - amplifyPush --simple

  - frontend:
      phases:
        preBuild:
          commands:
            - ls -al
            - node -v
            - npm ci
            - npm install --global npm@latest
        build:
          commands: 
            - npm run-script build
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths: 
          - node_modules/**/*
    appRoot: src
